# Copy to .env (git-ignored) for the optional live agent demo: npm run agent
# Nothing that produces a number in results/ needs any key. See docs/DEVELOPMENT.md.
OPENROUTER_API_KEY=
# OPENROUTER_MODEL=anthropic/claude-sonnet-5
