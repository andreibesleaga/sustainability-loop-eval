---
name: Reproduction problem
about: A result does not reproduce, a number in a document does not match results/, or npm test fails
title: "[repro] "
labels: reproduction
---

**Command that failed** (e.g. `npm test`, `npm run simulate` then `git diff results/`):

**Node version** (`node -v`; the package is tested on 22 and 24):

**Operating system:**

**What differed** (paste the failing assertion, or the `git diff --stat results/`):

**Commit or tag** (`git rev-parse HEAD`; the article's snapshot is `v1.0.0`):
